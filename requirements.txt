streamlit>=1.40
pandas>=2.2
plotly>=5.24
requests>=2.32
python-dotenv>=1.0
libsql-client>=0.3            # hosted SQLite (Turso) — pure Python, used when TURSO_* is set
anthropic>=0.49               # AI coaching (used only when ANTHROPIC_API_KEY is set)
